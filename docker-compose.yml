services:
  pear-to-pear:
    build: .
    restart: unless-stopped
    ports:
      - '127.0.0.1:8787:8787'
    env_file:
      - .env

# Bound to 127.0.0.1 on purpose: put a reverse proxy (Caddy/nginx/Cloudflare
# Tunnel) in front for TLS instead of exposing this port directly to the
# internet. See docs/DEPLOY.md.
